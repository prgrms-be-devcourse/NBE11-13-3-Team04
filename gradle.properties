org.gradle.caching=true

# 병렬 빌드를 켜지 말 것.
# 테스트가 공유 MySQL(iter_test)에 ddl-auto: create-drop 으로 붙는다.
# 모듈별 테스트가 동시에 돌면 두 JVM 이 같은 스키마를 지웠다 만들어 서로를 깨뜨린다.
# 모듈마다 DB 를 분리하거나 Testcontainers 를 도입한 뒤에 켤 것.
org.gradle.parallel=false
